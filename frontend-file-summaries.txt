{
  "frontend/src/App.tsx": "Main application entry point with TanStack Router; `AuthGuard` now shows a full-page loader while `isInitializing` is true (preventing premature redirects), and the router tree uses a nested `protectedRoute` with `ProtectedRoute` as the layout component for all authenticated pages.",
  "frontend/src/utils/encryption.ts": "AES-256-GCM client-side encryption/decryption utilities using the Web Crypto API with PBKDF2 key derivation; fixes TypeScript type errors by casting `Uint8Array.buffer` to `ArrayBuffer` explicitly.",
  "frontend/src/pages/SettingsPage.tsx": "Complete settings page with sections for profile editing, vault password change, email 2FA toggle, active sessions display, and account deletion; fixes the TypeScript error by passing `vaultPasswordHash: null` when updating profile info (preserving the existing password), and cleans up the duplicate `useInternetIdentity` import alias.",
  "frontend/src/data/defaultLinks.ts": "Defines default link categories, icon mappings, and category groups for the sidebar/dashboard; the mAadhaar URL is now set to 'https://uidai.gov.in/en/' and the Xiaomi Home URL is now set to 'http://play.google.com/store/apps/details?id=com.xiaomi.smarthome&hl=en_IN'.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; refactored to use `useActorReady` for gating all queries, includes the authenticated principal in all cache keys, treats Unauthorized/Anonymous errors as null (no profile) rather than error states, and exports a `profileNotFound` flag from `useGetProfile`.",
  "frontend/src/components/ProtectedRoute.tsx": "Guards authenticated routes with a strict state machine: shows a full-page loader during identity initialization or actor/profile loading, shows an error screen with Retry on fetch failure, redirects to `/login` only when definitively unauthenticated, shows `ProfileSetupModal` for new users, and renders `<Outlet />` when a valid profile exists.",
  "frontend/src/components/DashboardHeader.tsx": "Dashboard top bar with logo, secure-connection indicator, and profile dropdown; now shows a skeleton placeholder while the profile is loading instead of rendering stale/empty data.",
  "frontend/src/pages/DashboardPage.tsx": "Main dashboard view with welcome banner, stat cards, quick-access link grid, and vault notes preview; fixed TypeScript errors by using the correct `CategoryGroup` properties (`category.category` for the name, `category.icon` for the icon component, and `link.name` as the key instead of the non-existent `link.id`).",
  "frontend/src/components/ProfileSetupModal.tsx": "Registration modal for new users collecting name, username, optional email, and vault password; after successful registration it invalidates and refetches the profile query keys scoped to the current principal to trigger an immediate re-evaluation in `ProtectedRoute`.",
  "frontend/src/hooks/useActorReady.ts": "New hook that wraps `useActor` and `useInternetIdentity` to derive a stable `isActorReady` boolean — true only when identity initialization is complete, the user is authenticated (non-anonymous), and the actor is non-null and not fetching."
}