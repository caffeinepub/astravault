{
  "frontend/src/App.tsx": "Main application with TanStack Router; RootLayout uses a settled-flag pattern so brief re-initialization flashes don't re-show the loader; defines routes for /login, /, /vault, and /settings with ProtectedRoute and DashboardLayout wrapping protected pages.",
  "frontend/src/utils/encryption.ts": "AES-256-GCM client-side encryption/decryption utilities using the Web Crypto API with PBKDF2 key derivation; fixes TypeScript type errors by casting `Uint8Array.buffer` to `ArrayBuffer` explicitly.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page with profile update, vault password change, email 2FA toggle, and delete account with DELETE confirmation; updated to destructure `principalText` directly from `useActorReady()` instead of the removed `principal` property.",
  "frontend/src/data/defaultLinks.ts": "Default link categories (BANK, EMAIL, INVESTMENT, SERVICES, PAYMENTS) with all correct URLs; exports both new names (`iconMap`, `defaultLinkCategories`) and backward-compatible aliases (`ICON_MAP`, `AVAILABLE_ICONS`, `ALL_CATEGORIES`, `DEFAULT_CATEGORIES`).",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions (profile, custom links, vault notes, vault password, email 2FA); all queries gated on `isActorReady` with principal-scoped cache keys, and null profile responses are treated as \"new user\" not errors.",
  "frontend/src/components/ProtectedRoute.tsx": "Guards authenticated routes with a settled-flag pattern; shows military-themed loading spinners during initialization and profile loading, an error state with Retry button on failure, ProfileSetupModal for new users, and Outlet for authenticated users with profiles.",
  "frontend/src/components/DashboardHeader.tsx": "Dashboard top bar with AstraVault logo, sidebar toggle, secure connection indicator, and profile dropdown with name/avatar/logout (via `LogoutConfirmationModal`).",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page showing welcome banner, stat cards (default links, custom links, vault notes), quick access navigation, recent vault activity, and default links grid; no longer wraps content in DashboardLayout (layout is applied via route tree).",
  "frontend/src/components/ProfileSetupModal.tsx": "Full-page registration form for new users collecting name, username, optional email, and vault password (SHA-256 hashed); invalidates and refetches profile queries after successful registration, styled with matte black background and gold/military-green accents.",
  "frontend/src/hooks/useActorReady.ts": "Derives `isActorReady` boolean using a ref-based \"has ever settled\" flag so brief re-initialization flashes don't reset readiness; returns `true` only when init has settled, identity is non-anonymous, and actor is non-null; also exports `principalText`.",
  "frontend/src/pages/LoginPage.tsx": "AstraVault login page with military theme (matte black background, gold accents, military green), shield logo, feature badges, and a single \"Login with Internet Identity\" button; redirects authenticated users to `/` and shows a spinner while logging in or initializing.",
  "frontend/src/components/DashboardLayout.tsx": "Layout shell wrapping all protected pages with `DashboardHeader`, collapsible `AppSidebar`, textured main content area, and `DashboardFooter`.",
  "frontend/src/components/DashboardFooter.tsx": "Minimal footer with two-line gold attribution text: \"Developed by Shubham Rathore\" and \"Rajput – Creating History\".",
  "frontend/src/components/LogoutConfirmationModal.tsx": "Alert dialog confirming logout before clearing the session and redirecting to `/login`.",
  "frontend/src/components/AppSidebar.tsx": "Collapsible sidebar with navigation, categorized default/custom links, and \"+ Add Link\" button; renders `CustomLinkManager` conditionally with `onClose` prop instead of `open`/`onOpenChange`.",
  "frontend/src/pages/VaultPage.tsx": "Vault page with lock/unlock flow using hashPassword from encryption.ts, full CRUD for AES-256-GCM encrypted notes, vault password reset with note re-encryption; imports useGetProfile (not useGetCallerUserProfile) and renders without DashboardLayout wrapper.",
  "frontend/src/index.css": "Global styles with Version 4 OKLCH military theme tokens — pure matte black surfaces (`#0a0a0a`/`#111` mapped to `oklch(0.08)`/`oklch(0.11)`), deep military green accents (`#2d4a1e`/`#3a5c28`), and gold highlights (`#c9a84c`/`#d4af37`), with Rajdhani/Inter font imports and utility classes for theme tokens.",
  "frontend/tailwind.config.js": "Tailwind config extending theme with Version 4 military color tokens (gold-accent `#c9a84c`, military-green-primary `#2d4a1e`, surface-darkest `#0a0a0a`), Rajdhani/Inter fonts, sharp 0px border-radius, military box shadows, and required plugins.",
  "frontend/src/components/CustomLinkManager.tsx": "Modal dialog for managing custom links (add/edit/delete); updated to use `onClose` prop pattern (rendered conditionally by parent), `iconMap` instead of `ICON_MAP`, and `id` instead of `linkId` in the edit mutation payload.",
  "frontend/src/components/IconPicker.tsx": "Grid icon picker with search; updated to import `iconMap` and `AVAILABLE_ICONS` from the corrected `defaultLinks.ts` exports.",
  "frontend/src/main.tsx": "Application entry point that sets up QueryClient, InternetIdentityProvider with environment-aware identity provider URL (local vs production), and renders the App."
}