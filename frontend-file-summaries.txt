{
  "frontend/src/App.tsx": "Main application entry point that sets up TanStack Router with routes for login, dashboard, vault, and settings pages, with an AuthGuard component that redirects unauthenticated users to the login page and wraps authenticated routes with ProtectedRoute.",
  "frontend/src/utils/encryption.ts": "AES-256-GCM client-side encryption/decryption utilities using the Web Crypto API with PBKDF2 key derivation; fixes TypeScript type errors by casting `Uint8Array.buffer` to `ArrayBuffer` explicitly.",
  "frontend/src/pages/SettingsPage.tsx": "Complete settings page with sections for profile editing, vault password change, email 2FA toggle, active sessions display, and account deletion with confirmation modal.",
  "frontend/src/data/defaultLinks.ts": "Defines default link categories, icon mappings, and category groups for the sidebar/dashboard; the mAadhaar URL is now set to 'https://uidai.gov.in/en/' and the Xiaomi Home URL is now set to 'http://play.google.com/store/apps/details?id=com.xiaomi.smarthome&hl=en_IN'.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; `useGetCallerUserProfile` now catches authorization traps (new users without `#user` role) and returns `null` instead of throwing, so the ProfileSetupModal is shown correctly rather than an error screen.",
  "frontend/src/components/ProtectedRoute.tsx": "Guards authenticated routes by checking identity, actor/profile loading state, and profile existence; now includes a proper error state with a retry button, and uses `!userProfile` (covers both `null` and `undefined`) to reliably trigger the ProfileSetupModal for new users.",
  "frontend/src/components/DashboardHeader.tsx": "Dashboard top bar with logo, secure-connection indicator, and profile dropdown; now shows a skeleton placeholder while the profile is loading instead of rendering stale/empty data.",
  "frontend/src/pages/DashboardPage.tsx": "Main dashboard view with welcome banner, stat cards, quick-access link grid, and vault notes preview; fixed TypeScript errors by using the correct `CategoryGroup` properties (`category.category` for the name, `category.icon` for the icon component, and `link.name` as the key instead of the non-existent `link.id`)."
}