{
  "frontend/src/App.tsx": "Main application with TanStack Router; `RootLayout` uses a settled-flag pattern (ref + state) so that once `isInitializing` resolves to `false` for the first time, brief re-initialization flashes from the authClient dependency loop no longer re-show the \"Securing Connection\" loader.",
  "frontend/src/utils/encryption.ts": "AES-256-GCM client-side encryption/decryption utilities using the Web Crypto API with PBKDF2 key derivation; fixes TypeScript type errors by casting `Uint8Array.buffer` to `ArrayBuffer` explicitly.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page with profile editing, vault password change, email 2FA toggle, and account deletion (with DELETE confirmation); all sections use the military theme.",
  "frontend/src/data/defaultLinks.ts": "Default link categories (BANK, EMAIL, INVESTMENT, SERVICES, PAYMENTS) with all correct URLs; exports both new names (`iconMap`, `defaultLinkCategories`) and backward-compatible aliases (`ICON_MAP`, `AVAILABLE_ICONS`, `ALL_CATEGORIES`, `DEFAULT_CATEGORIES`).",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; all queries gated on `isActorReady` with principal-scoped cache keys, and `useEditVaultNote` uses `id` (not `noteId`) to match the mutation payload shape.",
  "frontend/src/components/ProtectedRoute.tsx": "Guards authenticated routes with a settled-flag pattern mirroring `App.tsx`; only blocks on the very first initialization pass, then checks actor readiness, authentication state, profile load, and renders `<Outlet />` or `<ProfileSetupModal />` accordingly.",
  "frontend/src/components/DashboardHeader.tsx": "Dashboard top bar with AstraVault logo, sidebar toggle, secure connection indicator, and profile dropdown with name/avatar/logout (via `LogoutConfirmationModal`).",
  "frontend/src/pages/DashboardPage.tsx": "Main dashboard with welcome banner, stat cards (quick links, custom links, vault notes), quick-access link grid by category, and recent vault notes preview.",
  "frontend/src/components/ProfileSetupModal.tsx": "Full-page registration form for new users collecting name, username, optional email, and vault password; invalidates and refetches profile queries after successful registration.",
  "frontend/src/hooks/useActorReady.ts": "Derives `isActorReady` boolean using a ref-based \"has ever settled\" flag so that brief re-initialization flashes (caused by the authClient dependency loop) don't reset readiness; returns `true` only when init has settled at least once, identity is non-anonymous, and actor is non-null.",
  "frontend/src/pages/LoginPage.tsx": "AstraVault login page with military theme (matte black background, gold accents, military green), shield logo, tagline, and a single \"Login with Internet Identity\" button; redirects authenticated users to `/`.",
  "frontend/src/components/DashboardLayout.tsx": "Layout shell wrapping all protected pages with `DashboardHeader`, collapsible `AppSidebar`, textured main content area, and `DashboardFooter`.",
  "frontend/src/components/DashboardFooter.tsx": "Minimal footer with two-line gold attribution text: \"Developed by Shubham Rathore\" and \"Rajput – Creating History\".",
  "frontend/src/components/LogoutConfirmationModal.tsx": "Alert dialog confirming logout before clearing the session and redirecting to `/login`.",
  "frontend/src/components/AppSidebar.tsx": "Collapsible sidebar with navigation, categorized default/custom links, and \"+ Add Link\" button; renders `CustomLinkManager` conditionally with `onClose` prop instead of `open`/`onOpenChange`.",
  "frontend/src/pages/VaultPage.tsx": "Secure vault page with lock/unlock flow and encrypted note CRUD; fixed imports to use `encryptNote`/`decryptNote` (the correct exported names from `encryption.ts`).",
  "frontend/src/index.css": "Global styles with OKLCH military theme tokens (matte black surfaces, military green accents, gold highlights), Rajdhani/Inter font imports, and utility classes for theme tokens.",
  "frontend/tailwind.config.js": "Tailwind config extending theme with military color tokens (gold-accent, military-green-primary/accent, surface-darkest/dark), Rajdhani/Inter fonts, sharp border-radius (0px), military box shadows, and required plugins.",
  "frontend/src/components/CustomLinkManager.tsx": "Modal dialog for managing custom links (add/edit/delete); updated to use `onClose` prop pattern (rendered conditionally by parent), `iconMap` instead of `ICON_MAP`, and `id` instead of `linkId` in the edit mutation payload.",
  "frontend/src/components/IconPicker.tsx": "Grid icon picker with search; updated to import `iconMap` and `AVAILABLE_ICONS` from the corrected `defaultLinks.ts` exports.",
  "frontend/src/main.tsx": "Application entry point that sets up QueryClient, InternetIdentityProvider with environment-aware identity provider URL (local vs production), and renders the App."
}